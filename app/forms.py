#from flask_wtf import Form
#from wtforms import StringField, validators, TextAreaField
#from wtforms.fields.html5 import DateField

"""
class AssessmentForm(Form):
    title = StringField('title', [validators.required(), validators.length(max=100)])
    code = StringField('code', [validators.required(), validators.length(max=10)])
    deadline = DateField('date') # stores datetime.date
    description = TextAreaField('description', [validators.Optional(), validators.length(max=1500)])
"""
